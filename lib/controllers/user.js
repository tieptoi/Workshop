'use strict';

var mongoose = require('mongoose'),
    User = mongoose.model('User');

/**
 * Get awesome things
 */

// exports.awesomeThings = User.findById(req.user, function (err, user) {
            //     res.send(user);
            // });
